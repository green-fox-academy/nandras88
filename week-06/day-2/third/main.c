#include <stdio.h>
#include <stdlib.h>

// - Create an array variable named `q`
//   with the following content: `[4, 5, 6, 7]`
// - Print the third element of `q`

int main()
{
	int q[] = {4, 5, 6, 7};
	printf("%d\n", q[2]);
	return 0;
}